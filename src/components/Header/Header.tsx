import TopBar from "./TopBar";
import Logo from "./Logo";
import NavBar from "./NavBar";

const Header = () => {
  return (
    <header>
      <TopBar />
      <Logo />
      <NavBar />
    </header>
  );
};

export default Header;
